apscheduler>=3.10,<4
beautifulsoup4>=4.12,<5
motor>=3.3,<4
playwright>=1.43,<2
playwright-stealth>=1.0.6,<2
resend>=0.7,<1
# MR.5 additions:
# - cryptography: hybrid decrypt path (RSA-OAEP + AES-GCM) for
#   GC-provided DOB NOW credentials. See lib/crypto.py.
# - redis: queue client (plain BRPOP pattern, not BullMQ — Python
#   BullMQ ecosystem too immature). See lib/queue_client.py.
# - httpx: heartbeat + /api/internal/* posts. Worker-only HTTP;
#   the existing bis_scrape handler also uses httpx via Playwright's
#   internal stack but doesn't import it directly.
cryptography>=42.0,<43
redis>=5.0,<6
httpx>=0.25,<0.28
