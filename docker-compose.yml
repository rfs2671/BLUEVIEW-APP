# MR.14 commit 4a — v1 monitoring product ships WITHOUT a local
# agent container. The dob_worker / cloudflared services that
# previously lived here are removed (renewal automation deferred
# to v2). LeveLog v1 is a pure monitoring + notification product
# served by the Railway backend; nothing runs on operator laptops.
#
# This file is preserved as an empty stub so any deployment
# automation that referenced docker-compose continues to no-op
# gracefully. Future v2 services would land here.

services: {}
